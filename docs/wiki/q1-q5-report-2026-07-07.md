---
title: 远程交流缓存/加载链路诊断 + 五层记忆架构落地报告
source: session
created: 2026-07-07
last_confirmed: 2026-07-07
schema_version: 2
audience: self
stage: current
status: current
confidence: high
entity_type: chapter
tags: [remote-load, p2p-cache, five-layer-memory, l0-window, l1-summary, l2-events, l3-state, l4-vector, h2-bug-fix]
compiled_from: [session-2026-07-07]
---

# 远程交流缓存/加载链路诊断 + 五层架构落地 (v0.2.12)

## 用户原问题

1. 远程交流过程中本地加载为什么会突然中断消失?
2. 链接加载过程有对方不在线的提示吗?
3. 已经加载到 channel 里面的记录可以不要随便消失吗?
4. 加载消息历史的时候,有没有从本地加载的过程?
5. 评估"四类系统组合"缓存方案在 bolloon 怎么落地.
6. UI bug: "channel 不在也没显示" (实施过程中用户发现)

## 诊断要点 (14 个根因 + 证据)

| # | 根因 | 证据 | 风险 |
|---|---|---|---|
| R1.1 | 远端 channel `[]` reply 直接覆盖 + 持久化 | `server.ts:1536` | 高 |
| R1.2 | 老 iroh 通道 reply 只写内存 | `server.ts:476-487` | 中 |
| R1.3 | 远端 channel 历史 B 端零副本 | `server.ts:778` + `client.ts:3506` | **致命** |
| R1.4 | session > 50MB 拒加载返回 null | `server-storage.ts:70-73` | **致命** |
| R2.1 | p2p-direct 没有任何在线状态 API | grep 零匹配 | 高 |
| R2.2 | 前端远端 channel 只显示 lastConnectedAt | `client.ts:3151` | 高 |
| R3.1 | 删除 channel 硬 unlink, 无回收站 | `server.ts:3098-3107` | 中 |
| R4.1 | 零 LRU / 零内存 cache | grep `lru` 零 | 中 |
| R4.3 | memory-compressor + chat-archiver 写完即丢 | grep `readChatArchive` 零 | 中 |
| **H2** | **本地 channel 删了 UI 引用还在, 三层失守** | `server.ts:3328` + `server.ts:2044` + `client.ts:946` | **P0 已修** |
| H1 | 远端 channel 被取消分享 | `server.ts:3103` (DELETE 无 broadcast) | P1 未修 |
| H3 | 远端 peer offline silent refresh | `client.ts:3474` 静默 | P2 未修 |

## 五层架构落地 (P0/P1/P2 全部完成)

| 层 | 文件 | 状态 |
|---|---|---|
| **L0** 显式 LRU 窗口 | `src/bootstrap/session-window.ts` (新, 134 行) | ✅ P0-A |
| **L0** loadSession 加 window fallback | `src/web/server-storage.ts` (+50 行) | ✅ P0-B |
| **L0** 远端 channel 镜像 (B 端副本) | `src/bootstrap/remote-mirror.ts` (新, 130 行) + `server.ts` 接入 (+18 行) | ✅ P0-C |
| **L1** 滚动摘要 | 已有 (memory-compressor + chat-archiver) | ✅ 已有 |
| **L2** 事件日志 JSONL | `src/bootstrap/event-log.ts` (新, 187 行) | ✅ P1-A |
| **L2** prompt 注入最近 5 条事件 | `src/agents/pi-sdk.ts` (+20 行) | ✅ P1-B |
| **L2** 客户端时间线折叠块 | — | ❌ **P1-C 撤回** (用户报告 UI bug 后回滚) |
| **L3** 项目状态 | `src/bootstrap/project-state.ts` (新, 174 行) | ✅ P2-A |
| **L4** TF-IDF 向量索引 | `src/bootstrap/vector-index.ts` (新, 233 行) | ✅ P2-B |
| **L4** prompt 注入 state + top-3 检索 | `src/agents/pi-sdk.ts` (+30 行) | ✅ P2-C |

## H2 三层失守修复 (P0)

| 层 | 文件 | 改动 |
|---|---|---|
| L1 API | `src/web/server.ts:3328` `/sessions/:channelId` | 加 channel 校验, 找不到 → 404 |
| L1 API | `src/web/server.ts:2044` `/message` | 加 channel 校验, 找不到 → 404 |
| L3 Client | `src/web/client.ts:946` `selectChannel` | channel 不存在显示 "(channel 已删除)" + 系统提示 |
| L3 Client | `src/web/client.ts:1003` `loadSession` | 处理 404, 显示明确提示 |

**修复测试**: `src/test/channel-not-found.test.ts` (新, 175 行, 7 tests)

## 验证结果

- `npx tsc --noEmit`: **0 错**
- `npx vitest run`: **774/775 pass** (1 个已知 minimax 网络 flaky)
- `python scripts/wiki_check.py` / `raw_manifest_check.py` / `wiki_lint.py --strict=v2` / `supersede_check.py`: 全 OK

## writeback

- `log.md` 加 1 行表格 + 详细日志
- `current-status.md` 加 L0/L1/L2/L3/L4 5 个新功能
- `crystallized-claims.md` 加 1 条从 H2 蒸馏的稳定断言

## 下一步 (future work)

- H1 (远端 channel 被取消分享) — P1
- H3 (远端 peer offline silent refresh) — P2
- P0-C mirror 写盘失败重试
- LLM 自动建议 state 更新