--- a/src/utils/clamp.ts
+++ b/src/utils/clamp.ts
@@ -1,3 +1,6 @@
 export function clamp(n: number, min: number, max: number): number {
-  return n;
+  if (n < min) return min;
+  if (n > max) return max;
+  return n;
 }
