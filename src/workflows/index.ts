/**
 * Multi-Agent Collaboration Workflows
 *
 * 工作流设计文档：
 *
 * ┌─────────────────────────────────────────────────────────────┐
 * │                      用户输入                              │
 * └─────────────────────────────────────────────────────────────┘
 *                            │
 *                            ▼
 * ┌─────────────────────────────────────────────────────────────┐
 * │                   命令解析器 (CommandParser)                │
 * │  - 识别动作: read/summarize/improve/query/collaborate      │
 * │  - 提取目标文件、需求描述                                  │
 * │  - 检测协作关键词                                         │
 * └─────────────────────────────────────────────────────────────┘
 *                            │
 *                            ▼
 * ┌─────────────────────────────────────────────────────────────┐
 * │                  DID注册表 (DIDRegistry)                    │
 * │  - 管理已知智能体的DID信息                                │
 * │  - 从IPNS解析其他智能体DID文档                            │
 * │  - 追踪在线状态                                           │
 * └─────────────────────────────────────────────────────────────┘
 *                            │
 *                            ▼
 * ┌─────────────────────────────────────────────────────────────┐
 * │                 协作决策器 (CollaborationDecider)           │
 * │  - 基于任务复杂度、关键词、质量评分决策                     │
 * │  - 选择合适的执行智能体                                   │
 * └─────────────────────────────────────────────────────────────┘
 *                            │
 *              ┌─────────────┴─────────────┐
 *              │                           │
 *              ▼                           ▼
 *    ┌─────────────────┐       ┌─────────────────┐
 *    │    独立执行      │       │   多智能体协作   │
 *    │  - 直接处理      │       │                 │
 *    │  - 返回结果      │       │  - 分发任务     │
 *    └─────────────────┘       │  - 收集结果     │
 *                              │  - 汇总汇报     │
 *                              └─────────────────┘
 *                                       │
 *                                       ▼
 *                              ┌─────────────────┐
 *                              │   任务分发器     │
 *                              │ (TaskDispatcher) │
 *                              │                 │
 *                              │ - Protocol发送  │
 *                              │ - 任务追踪      │
 *                              │ - 结果收集      │
 *                              └─────────────────┘
 */

export {
  CommandParser,
  DIDRegistry,
  CollaborationDecider,
  TaskDispatcher,
  MultiAgentWorkflow,
  type AgentInfo,
  type CollaborationResult,
  type CommandIntent
} from './collaboration.js';
