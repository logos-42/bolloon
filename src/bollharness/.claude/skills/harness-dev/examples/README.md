# harness-dev examples

This directory contains example implementations and patterns for the harness-dev skill.

## Examples

### Example 1: Basic Implementation

See `examples/basic-implementation/` for a simple example of implementing a feature following harness conventions.

### Example 2: Contract Change

See `examples/contract-change/` for an example of making a contract-level change with proper consumer tracking.

### Example 3: Parallel Execution

See `examples/parallel-execution/` for an example of executing multiple work tracks in parallel.
